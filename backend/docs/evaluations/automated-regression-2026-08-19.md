# 自动化回归报告（2026-08-19）

## 本轮目的

在真实千问接入、20案例过程题库、40题检索题库和证据存在性判断加入后，重新验证第一天数据库与检索成果、第二天工单与协调助手主链路、第三天评测器，以及默认跳过的真实E5和百炼联网测试。

## 第一轮：默认全量回归

命令：

```powershell
npm.cmd test
```

结果：

| 总数 | 通过 | 失败 | 跳过 | 耗时 |
|---:|---:|---:|---:|---:|
| 214 | 206 | 0 | 8 | 约109.9秒 |

默认跳过项：

- R133：真实`multilingual-e5-small`本地模型；
- R182：真实千问选择官方知识检索；
- R183：真实千问根据证据草拟完整方案。
- R187：真实千问执行20案例、40轮协调决策评测。
- R196：真实千问对E5前三候选执行二次排序评测。
- R203：真实E5在冻结24题留出集上验证0.86无答案候选阈值。
- R215：真实千问在已经暴露结果的24题上验证证据判断回归。
- R221：固定E5和真实千问在首次运行前封存的36道新题上验证证据判断推广效果。

它们默认跳过是为了避免普通回归依赖本地大模型缓存或产生外部模型调用费用，不表示测试代码缺失。其中R196虽然已经有适配器、评测器和预冻结门槛，但现有Token Plan密钥不能用于业务空间后端排序，所以不能声称已通过真实模型验收。R203已经显式运行且失败；R215和R221已经显式运行且通过。跳过只避免每次默认回归重复调用，不改变各自真实状态。

## 第二轮：显式真实模型测试

### 真实E5

```powershell
$env:RUN_E5_INTEGRATION = '1'
$env:HF_LOCAL_ONLY = '1'
node --test tests/e5-embedder.integration.test.ts
```

结果：R133通过，0失败，耗时约0.9秒。验证384维、归一化以及相关文本相似度高于无关文本。

### 真实千问

加载外部百炼配置到当前进程后执行：

```powershell
$env:RUN_QWEN_INTEGRATION = '1'
node --test tests/qwen-coordinator.integration.test.ts
```

结果：

| 规则 | 场景 | 结果 | 本次耗时 |
|---|---|---|---:|
| R182 | 选择官方知识检索并返回合法查询词 | 通过 | 约1.4秒 |
| R183 | 只根据给定证据生成字段完整的低风险方案 | 通过 | 约5.6秒 |

### 真实千问20案例过程评测

加载同一份外部百炼配置到当前进程后执行：

```powershell
$env:RUN_QWEN_TRAJECTORY_EVAL = '1'
node --test tests/qwen-agent-trajectory.integration.test.ts
```

结果：R187通过，0失败，真实40轮总耗时约66.1秒。结构输出100%、动作正确92.5%、参数正确92.5%、整条过程85%，四项均达到联网前冻结门槛。20个案例中17个完整通过，三个失败及其编排含义见[正式过程评测报告](qwen-agent-trajectories-v1-2026-08-19.md)。

### 真实E5无答案留出门

```powershell
$env:RUN_E5_ANSWERABILITY_EVAL = '1'
$env:HF_LOCAL_ONLY = '1'
node --test tests/e5-answerability-holdout.integration.test.ts
```

结果：R203未通过。可回答正确接收91.7%达到75%门槛；无答案拒答83.3%低于100%门槛；接收准确率84.6%低于90%门槛。题库、0.86阈值、三项门槛和SHA在首次运行前已经冻结，失败后没有改题或调阈值，机器报告为`reports/ohf-answerability-holdout-v1-result.json`。

### 证据存在性判断：旧题回归

改为E5前五候选加独立千问证据判断，并由程序核对候选编号、页码和引用原文后，显式运行R215。旧24题得到有答案正确放行100%、无答案拒答100%、放行准确率100%、总体100%、判断错误0次。由于这些题已经参与过失败分析，该结果只证明已知错误修复，不能称为新未见数据成绩。

### 证据存在性判断：新36题首次运行

R217—R220在联网前确认36题为18正18负、六候选各3题、与旧40题及24题没有重复，并冻结题库、候选资料、判断器代码、E5版本、千问模型、提示版本、前五候选和四项门槛。R221首次真实运行得到有答案正确放行94.4%、无答案拒答100%、放行准确率100%、总体97.2%、判断错误0次，四项门槛全部通过。唯一错误N14是保守拒答。题目由项目组编写，仍不是真实工厂数据。

## 汇总结论

本节数字是证据门主链路接入前的历史快照：当时214个测试定义，默认回归为206通过、0失败、8项按设计跳过。此后证据判断已接入工单主链路，并新增默认跳过的真实主链路R228；最新数字以README和本轮完整测试输出为准。R196仍是在模型计算前因后端密钥授权域不匹配返回401，R203仍是0.86阈值真实未达标，二者不能记为通过。

40题开发评测已经完成：30道可回答题中，E5首位23/30、前三30/30；门控混合首位22/30、前三30/30。产品族前置门把5道错ATV系列问题全部拒答并避免向量调用；5道当前语料无答案问题仍全部被E5/混合强行匹配，说明拒答能力尚未完成。

同轮执行`npm audit --omit=dev`仍报告4项高危问题，依赖链为`onnxruntime-node → adm-zip`和`@huggingface/transformers → sharp`，上游当前没有自动修复版本。当前文字检索不接收用户压缩包或图片，但正式部署仍需升级、替换或隔离本地推理运行时。

这里的“全部通过”只针对现有自动化规则。它不能替代：

- 来自真实工厂查询日志或与开发集隔离的新盲测；
- 真实模型实际驱动窄工具并检查数据库终态的端到端评测；
- 真实领域工程师对知识片段和工业方案的审核；
- 服务端PostgreSQL并发、权限和故障恢复测试；
- 真实工厂现场验收。

## 本轮代码变化

新增R184—R187、20案例40轮固定题库、独立过程评测器、真实联网入口和逐轮机器可读报告；R188先重现同一输入被重复记录到步数上限，再增加运行内单次消费规则。随后新增R189—R197、40题三轴检索题库、排序适配器、业务结果指标和错产品族前置门；R198—R200增加输入高危意图门；R201—R203增加24题留出集、阈值计分器和真实E5能力门；R204增加模型测试登记表与源码交叉检查；R205—R216建立前五候选证据判断、程序引用校验、独立计分和旧题回归；R217—R221建立新36题、预运行封存和首次真实能力门；R222—R229把证据门接入工单主链路；R230—R236建立四分支终态封存、保留3/4首轮失败并以单变量分类边界修复到已见回归4/4。最新默认全量为229项、218通过、0失败、11项按设计跳过，耗时约112.9秒。数据库既有身份、来源和工单状态规则未被放宽，新增迁移023只扩展版本化安全规则种类。
